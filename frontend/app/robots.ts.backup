import { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/studio/', // Sanity Studio public users ले नदेखुन्
    },
    sitemap: 'https://www.tikajoshi.com.np/sitemap.xml',
  };
}